# Supabase
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_SERVICE_KEY=sua-service-role-key
# ou use SUPABASE_ANON_KEY se preferir

# Servidor (OBRIGATÓRIO — porta 3000 é usada pelo Vite frontend)
PORT=3001

# JWT - IMPORTANTE: Mude esta chave em produção!
# Use uma chave aleatória de pelo menos 32 caracteres
JWT_SECRET=sua-chave-secreta-super-segura-mude-em-producao-minimo-32-caracteres-aleatorios

# Token único da API (opcional)
# Se não definido, será gerado automaticamente na primeira execução
# Para gerar um token, execute: bun run token:generate
# API_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Configuração do token único (opcional)
# API_TOKEN_IDENTIFIER=upmap-api
# API_TOKEN_EXPIRES_IN=10y
