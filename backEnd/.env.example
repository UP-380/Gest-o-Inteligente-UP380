# =============================================================
# VARIÃVEIS DE AMBIENTE - UP GESTÃƒO INTELIGENTE
# =============================================================
# 
# INSTRUÃ‡Ã•ES:
# 1. Copie este arquivo para .env
# 2. Preencha os valores das variÃ¡veis abaixo
# 3. NUNCA commite o arquivo .env no Git!
#
# =============================================================

# Ambiente (development, production)
NODE_ENV=development

# Porta do servidor (padrÃ£o: 4000)
PORT=4000

# Host do servidor (padrÃ£o: 0.0.0.0)
HOST=0.0.0.0

# Porta do servidor de dashboard de clientes (padrÃ£o: 4001)
PORT_DASHBOARD_CLIENTES=4001

# =============================================================
# SUPABASE - BANCO DE DADOS
# =============================================================

# URL do projeto Supabase
SUPABASE_URL=https://seu-projeto.supabase.co

# Service Key do Supabase (NUNCA compartilhe esta chave!)
SUPABASE_SERVICE_KEY=sua-service-key-aqui

# Schema do Banco de Dados (padrão: up_gestaointeligente)
# Use 'up_gestaointeligente_dev' para desenvolvimento local
SUPABASE_DB_SCHEMA=up_gestaointeligente

# =============================================================
# UPLOAD DE ARQUIVOS
# =============================================================

# Caminho para upload de avatares (opcional)
# Se nÃ£o definido, usa o caminho padrÃ£o baseado no NODE_ENV
UPLOAD_AVATAR_PATH=/caminho/para/uploads
